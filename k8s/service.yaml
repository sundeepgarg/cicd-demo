apiVersion: v1
kind: Service
metadata:
  name: cicd-demo
spec:
  selector:
    app: cicd-demo
  ports:
  - port: 80
    targetPort: 5000
  type: LoadBalancer   # Azure allocates a public IP automatically
